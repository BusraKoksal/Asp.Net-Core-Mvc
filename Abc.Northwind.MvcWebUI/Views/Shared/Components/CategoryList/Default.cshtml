@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model Abc.Northwind.MvcWebUI.Models.CategoryListViewModel
@{
}
<div class="list-group">
    @foreach(var category in Model.Categories)
    {

        var css = "list-group-item";
        if (category.CategoryId == Model.CurrentCategory)
        {
            css += "active";
        }
        <a href="/product/index?category=@category.CategoryId" class="@css">@category.CategoryName  </a>
       
    }
</div>













@* href="#"->Eğer href özelliğine # sembolü atanırsa,
    bu bağlantının tıklanması durumunda sayfa yukarı
    kaydırılır ve sayfanın başına döner.---> list-group-item; sınıfı eklenerek, Bootstrap'in sağladığı stil ve düzenleme
    özelliklerinden yararlanılıyor.list-group-item sınıfı, aynı zamanda içeriği daha ayrıntılı ve zengin hale
    getirmek için diğer Bootstrap bileşenleriyle birleştirilebilir.
    Örneğin, düğmeler, başlık etiketleri, resimler vb. ile kullanarak özelleştirilmiş liste öğeleri oluşturabilirsiniz.*@
